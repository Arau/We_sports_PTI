<?php
	$lifetime = 7200;
	if (isset($_POST['user']['remember_me'])) {
		$lifetime *= 12;
	}

	ini_set("session.gc_maxlifetime", $lifetime);
	@session_start();
	setcookie(session_name(),session_id(),time()+$lifetime);

	$uname  = $_POST['user']['username'];
	$passwd = $_POST['user']['password'];
	
	if (session_start() == ;
	
	
?>
